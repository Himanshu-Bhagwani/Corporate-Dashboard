{
  "name": "soda-corporate-dashboard",
  "version": "1.0.0",
  "description": "SODA Corporate Dashboard - Live Production Deployment Configuration",
  "main": "backend/server.js",
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --production=false --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run install:backend && npm run install:frontend && npm run build:frontend",
    "postinstall": "npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
